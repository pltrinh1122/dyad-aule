# N3 — d-commission intake discipline

NODE N3 deps=N0 done=d-commission.sh lane=agent :: d-commission intake discipline — brief → spine → executable acceptance criteria → regime check → freeze → decompose into enqueued nodes (G1; the queue's producer)

## Intent

The queue's **producer**: the discipline that turns an Operator brief into enqueued nodes —
brief → elicit the spine → executable acceptance criteria → regime check (can this job tolerate a
mistake? if not → refuse to careful-human) → freeze the intent → decompose into new plan nodes.
This is where the craft's two-payload telos is paid once (outcome + criteria) and the queue
grows. The sanctioned async intake inbox (issue comments, Operator-filed issues) feeds this
discipline (*inbox, never truth*) into replanning PRs.
