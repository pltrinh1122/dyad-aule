# N0 — Write the plan (the G0 dog-food node)

NODE N0 deps=- done=plan-dag.sh lane=agent :: Write this plan — the DAG map itself, the plan/ kind-home, and the DAG-shape criteria (the G0 dog-food node)

## Intent

The plan's own writing is node N0 of its own DAG — the dog-food property: the queue proves
itself by being the first thing the queue holds. This node stands up the `plan/` kind-home, the
node format (intent + edges + derived done-condition), and the shape criteria (`plan-dag.sh`)
that re-prove the DAG on every merge.

Source of the node set: the readiness evaluation
(`dialectic/2026-07-07-build-commission-readiness.md`, close-order §3, as amended by the
Operator's G0 falsification). Shape constraints inherited from the ratified concurrency
evaluation (`dialectic/2026-07-06-concurrency-fsm-evaluation.md` §4-5): DAG of orthogonal
nodes · state **derived, never stored** · WIP=1 · grounded on the remote via branch→PR→merge.

The contract for how to read node state lives in [`close-the-gaps.md`](close-the-gaps.md).
