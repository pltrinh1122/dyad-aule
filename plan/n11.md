# N11 — DECIDE the plan-node home (grounded by the FR)

NODE N11 deps=N0 done=disposition lane=operator :: DECIDE the plan-node home — local repo-files vs GitHub issues (grounded by dialectic/2026-07-08-plan-home-local-vs-issues.md; re-opens N9's mirror framing and N10's grain if it flips to issues)

DISPOSITION N11: RULED 2026-07-08 — ISSUES-AS-HOME. gh-issues are the node INTENT home (their off-device durability + portability is the dominant capability); the local plan becomes a synced CACHE for the rare offline case. Lifecycle status is STORED (`status:*` labels — re-derivation is too expensive, landing in C2's own falsification bound), but kept honest as a CHECKED cache by a real FSM state-guard (prevent illegal transitions + flag drift against derived ground). The real-half (criteria + `./check` + merge-gate; `DONE ⟺ criteria-green ∧ PR-merged`, C3) is UNCHANGED — C3 becomes the guard's reconciliation ground, not the storage rule. Replan enqueued as N12–N18 (guard-first). See "Ruling" below.

## Intent

An Operator decision-node (lane=operator), like N2. The choice — **is the durable home of a
plan-node a local repo-file (`plan/<id>.md`, aule today) or a GitHub issue (dyad-chiron)** — was
found **asserted, not grounded**: the reasoning lives only in `plan/close-the-gaps.md` (a contract
file, no `falsification_target`), and §0.5 of the readiness DR borrowed the concurrency eval's
*derive-vs-store* ratification (C2/C3) to license a *different* axis it never adjudicated (node
home). dyad-chiron is a live counter-instance (same operator + model, chose issues-as-home, works).

The agent's Sense input for this disposition is the falsifiable report
`dialectic/2026-07-08-plan-home-local-vs-issues.md` — it steelmans both sides, isolates the real
differentiator, and carries `falsification_target`s. The Operator reviews, falsifies, and rules;
the `DISPOSITION N11:` line above is the ruling (non-TODO ⟺ DONE), sealed by merge.

**Downstream:** if the ruling flips the home to issues, it re-opens N9 (the mirror collapses into
"the nodes themselves") and N10's per-file grain — those become replanning follow-ons. If it
confirms local-files, the record is upgraded from asserted to grounded (the decision earns its
keep). Either way the missing record now exists.

## Ruling (Operator, 2026-07-08)

The Operator ruled the §5 conditional toward **issues-as-home**: *"offline, necessary on rare
conditions, should operate like a cache, while the durability and portability of gh-issues are the
dominant capability we require."* And on the state axis: *"re-deriving lifecycle state is expensive,
therefore `status:*` is used; the risk is that `status:*` drifts, therefore we need state-management
guards to flag and prevent illegal state transitions."*

This lands squarely in **C2's own falsification bound** (`2026-07-06-concurrency-fsm-evaluation.md`:
"a substrate where done-ness cannot be cheaply re-derived → caching is unavoidable"), so it does not
violate C2 — it enters the regime C2 excluded. The obligation it creates is aule-specific: storing
status is the **wu-wei** failure mode (drifting pointers) and a paper state-guard is the **cairn**
failure mode (an FSM guard asserted but never wired). aule may store status **only** behind a guard
that is *real* — wired into the mutation path and criteria-proven (deny-illegal **and** allow-legal,
the `dyad-rt.sh` discipline). Synthesis: **stored status is a cache; the derived ground is its
check** — C3 is retained as the drift-detector, not discarded.

**The flip (locked):** node intent+edges → gh-issues; frontier/DAG derivation reads issues (via
`bin/gh`/API) with a local cache for offline; the N9 mirror reverses (issues→cache); the real-half
and merge-gate are untouched (truth about *real* never leaves the gate). Replan = N12–N18, executed
**guard-first** (N14 gates everything that writes status). This ruling, sealed by merge, is the
ratification anchor for that replan.
