# N14 — The state guard (FSM) ★ guard-first ★

NODE N14 deps=N13 done=state-guard.sh lane=agent :: Real FSM state-guard for stored status:* — prevent illegal transitions AND flag drift against derived ground; wired into bin/gh + CI, criteria-proven (deny-illegal ∧ allow-legal)

## Intent

**The load-bearing node — nothing may store status until this is real.** Storing `status:*` is the
wu-wei drift trap and a paper guard is the cairn counterfeit; aule may store only behind a guard
that is *wired* and *criteria-proven*. Two halves:

- **Prevent illegal transitions** (enforcer, single-homed like `bin/_dyad-rt`, wired into `bin/gh`
  label mutations + a CI gate): legal set is `clarify→dispose` (d-sense converges) · `dispose→execute`
  (d-land) · `execute→closed` (real-half met) · `*→blocked`/`blocked→*` (a `Depends-on` flips) ·
  `execute|dispose→clarify` (reground). Deny `clarify→execute` (no Act without disposition — op-intent
  / op-provenance), deny `→closed` unless `criteria-green ∧ PR-merged` (C3), deny >1 `status:*`.
- **Flag drift** (reconciler in CI + a `d-start` seam): `closed` but not criteria-green ⇒ drift ·
  `execute` with no open PR carrying the `Node:` trailer ⇒ drift · `blocked` but deps done ⇒ stale ·
  `dispose` but a dep not done ⇒ should be blocked. **Stored status is a cache; derived ground is its
  check** — C3 is the reconciliation ground.

`criteria/state-guard.sh` proves it in a throwaway fixture: it must DENY the illegal transitions AND
ALLOW the legal ones (a guard that denies everything proves nothing — the `dyad-rt.sh` discipline).
deps=N13 (needs the states/schema). Gates N15, N17, N18 — everything that writes or reads status.
