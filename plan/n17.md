# N17 — d-sense discipline (clarify → dispose)

NODE N17 deps=N14 done=d-sense.sh lane=agent :: The d-sense operator discipline — chat elicitation that drives a node's Sense to convergence (Interpretation-block playback + Operator rulings), closing clarify→dispose; grounded in dyad-chiron, adapted to aule

## Intent

The Operator's **primary clarification channel** (chat; issue-comments are the secondary path).
`d-sense <node>` runs op-intent's Sense turn, node-scoped: I play back inferred **intent · governing
invariants · gap-filling assumptions**, the Operator rules/strikes, we converge — and the converged
intent lands in the node-issue body. Closure = the `clarify → dispose` transition (guarded by N14;
in chiron this is the lint-gated `bin/ws converge`). A `d-*` operator discipline (O8) — joins
`d-start` in the allowlist; ships with `bin/d-sense` + `criteria/d-sense.sh`.

deps=N14 (the clarify→dispose transition it drives must be guard-enforced). Pairs with N18 (d-land
takes over at dispose→execute).
