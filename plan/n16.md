# N16 — Supersede the file-home (N10 grain + "why-not-issues")

NODE N16 deps=N15 done=plan-home-migrated.sh lane=agent :: Migrate the live plan-nodes into issues and retire the file-home artifacts — the per-file grain (N10) and the "Why the nodes are not GitHub issues" decision are struck as superseded by N11

## Intent

Close the flip: migrate the still-live plan-nodes (the open ones) into node-issues under the N13
schema, and **retire the file-home** — the per-file grain criteria (`plan-grain.sh`) and the
"Why the nodes are not GitHub issues" section become **superseded** (struck, with a tombstone
pointing at N11 so the reversal is legible, never silently dropped). `plan-dag.sh` either retargets
to the issues source or is retired in favor of N14's guard. `criteria/plan-home-migrated.sh` asserts
the migration is complete and the old home is retired without loss.

deps=N15 (don't strike the file-home until the issues-home + offline cache are working — no gap
where the plan has no home). This node makes N10 and the old §decision honestly obsolete rather than
contradictory.
