# N12 — Anchor flip: op-SoT (inbox → issues are the intent home)

NODE N12 deps=N11 done=sot-issues-home.sh lane=agent :: Flip op-SoT in DYAD.md — gh-issues become the node INTENT home; status is stored-and-reconciled (not derived); update the op-invariants criteria to match

## Intent

Land the ruling in the anchor. `op-SoT` currently says "external mutable stores … may serve as
intent inboxes, **never as truth**." The flip inverts it: **gh-issues ARE the node intent home**
(durable + portable, the dominant capability), with the local plan a synced cache; lifecycle
**status is stored** (`status:*`) as a checked cache, guarded by N14. The real-half / merge-gate
stay the truth-ground for *real* (C3 unchanged).

The anchor change is the Operator's lane (channel #4) — I draft the wording; the **merge ratifies**
it. `criteria/op-invariants.sh` (which currently asserts "inbox, never truth") is updated in lock so
the engine matches the anchor. Deps N11 (the ruling). Unlocks nothing directly — it is the anchor
half of the flip that the mechanism nodes (N13/N14) realize.
